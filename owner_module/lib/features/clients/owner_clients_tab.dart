import 'package:flutter/material.dart';

class OwnerClientsTab extends StatelessWidget {
  const OwnerClientsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Клиенты'),
    );
  }
}